- name: Demo on prompt
  hosts: localhost
  connection: local
  vars:
    num: 2
  tasks:
   - name: check even or odd
     when: num%2 == 0
     debug:
      msg: "give no is even {{ num }}"
   - name: check even or odd
     when: num%2 != 0
     debug:
      msg: "give no is odd {{ num }}"