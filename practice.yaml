- name: install nginx
  hosts: localhost
  connection: local
  tasks:
  - name: installing package
    package:
      name: nginx
      state: present
  - name: start nginx
    service:
    - name: nginx
      state: started
      enabled: yes
