- name: Demo on variable
  hosts: localhost
  connection: local
  vars:
   name: "srikanth"
   age: 20
   place: "kurnool"
  tasks:
  - name: taking variables 
    debug:
    msg: my name is {{ name }}, age is {{ age }}, from{{ place }}
