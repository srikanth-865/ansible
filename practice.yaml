- name: Demo on prompt
  hosts: localhost
  connection: local
  become: yes
  tasks:
   - name: installing multiple packages
     package:
      name: "{{ item.name }}"
      state: "{{item.state }}"
     loop:
      - { name:"Rhel" , state:"present"}
      - { name:"sk" , state:"notthere"}
      - { name:"sui" , state:"absent"}
      
    