- name: Demo on variable
  hosts: localhost
  connection: local
  vars:
   name: "srikanth"
   age: 20
   place: "kurnool"
  tasks:
  - name: printing task levele variables
    vars:
     name: "sreedhar"
     age: 20
     place: "devamada"
  - name: taking variables 
    debug:
       msg: my name is {{ name }}, age is {{ age }}, from{{ place }}
