- name: Demo on prompt
  hosts: localhost
  connection: local
  vars:
   session: 6
  tasks:
  - name: cheching ansible sessions
    when: session > 5
    debug:
     msg: " yeah it take 3 more sess"
     