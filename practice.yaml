- name: Demo on prompt
  hosts: local
  connection: local
  vars:
   session: 4
  tasks:
  - name: cheching ansible sessions
    when: session > 5
    debug:
     msg: " yeah it take 3 more sess"
     