- name: Demo on prompt
  hosts: local
  connection: local
  tasks:
   - name: printing the inventiry file local group variables
     debug:
       msg: " course name is {{ course }} trainer is {{ trainer }} time is {{ time }} common key is {{ common_variable }}"
  

