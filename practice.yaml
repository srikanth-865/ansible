- name: install nginx
  hosts: localhost
  connection: local
  tasks:
   - name: install package
     package:
      name: nginx
      state: present
   - name: serivce start
     service:
      name: nginx
      state: started
      enabled: yes
