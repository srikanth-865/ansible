- name: Demo on prompt
  hosts: localhost
  connection: local
  tasks:
   - name: 
     when: ansible_facts.os_family == "RedHat"
     debug:
       msg: "dnf"
   - name: 
     when: ansible_facts.os_family == "ubantu"
     debug:
       msg: "aptget"
